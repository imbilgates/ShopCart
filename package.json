{
  "name": "shoppingcart",
  "version": "1.0.0",
  "description": "this apllication for shopping-cart",
  "license": "ISC",
  "author": "bilgates",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server/server.js",
    "build": "npm install --prefix ./client && npm run build --prefix ./client && npm install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.9.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
